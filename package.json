{
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "firebase emulators:start --config=backend/firebase.json --import=backend/emulator-data --export-on-exit=backend/emulator-data --project=ekisde-fe4e1 --only functions,firestore,auth"
  }
}
